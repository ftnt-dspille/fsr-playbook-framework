[pytest]
testpaths = tooling/tests fsr_playbooks/tests
markers =
    slow: corpus-wide tests; deselect with -m "not slow"
    live: hits the FSR appliance — needs FSR_BASE_URL/FSR_USERNAME/FSR_PASSWORD; deselect with -m "not live"
addopts = -ra --tb=short
