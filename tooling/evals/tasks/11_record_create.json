{
  "name": "record_create",
  "prompt": "Author a FortiSOAR playbook with a manual trigger that takes an 'alert_name' parameter and creates an alert record. The new alert's description should include the marker 'fsrpb_e2e_create' so test runs can be cleaned up. Output complete YAML.",
  "gold_yaml_path": "examples/demo_record_create.yaml",
  "notes": "create_record step",
  "ir_assertions": [
    {
      "kind": "step_type_present",
      "type": "create_record",
      "note": "creates the alert"
    },
    {
      "kind": "arg_text_contains",
      "type": "create_record",
      "contains": "fsrpb_e2e_create",
      "note": "carries the cleanup marker the prompt demands"
    }
  ]
}
