{
  "name": "for_each_loop",
  "prompt": "Author a FortiSOAR playbook that builds a static list of three alert payloads in a set_variable step, then runs a for_each loop over the list using create_record on the alerts module. Each iteration uses vars.item to populate fields. Output complete YAML.",
  "gold_yaml_path": "examples/demo_for_each.yaml",
  "notes": "for_each + non-bulk create_record",
  "ir_assertions": [
    {
      "kind": "step_type_present",
      "type": "set_variable",
      "note": "builds the list"
    },
    {
      "kind": "step_type_present",
      "type": "create_record",
      "note": "creates alerts"
    },
    {
      "kind": "for_each_over",
      "contains": "vars.steps.",
      "note": "loops over the list the earlier step built"
    },
    {
      "kind": "arg_text_contains",
      "type": "create_record",
      "contains": "vars.item",
      "note": "each iteration uses vars.item"
    }
  ]
}
