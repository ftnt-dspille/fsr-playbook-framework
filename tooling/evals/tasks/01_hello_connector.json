{
  "name": "hello_connector",
  "prompt": "Author a manual-trigger FortiSOAR playbook called 'Hello Connector' that runs the smtp connector's 'send_email' operation with a fixed `to`, `subject`, and `body`. End the playbook with an `end` step. Output the complete YAML.",
  "gold_yaml_path": "examples/hello_connector.yaml",
  "notes": "Smallest possible connector-call playbook. Tests Compiles + gold.",
  "ir_assertions": [
    {
      "kind": "arg_text_contains",
      "contains": "send_email",
      "note": "runs the send_email operation (as a connector step or the send_email step type)"
    },
    {
      "kind": "arg_text_contains",
      "contains": "smtp",
      "note": "via the smtp connector"
    },
    {
      "kind": "step_type_present",
      "type": "end",
      "note": "ends with an end step"
    }
  ]
}
