{
  "name": "virustotal_ip",
  "prompt": "Author a FortiSOAR playbook with a manual trigger that takes an 'ip' parameter, calls the virustotal connector's IP reputation operation, then runs a Decision branching on last_analysis_stats.malicious count, and stamps a verdict via set_variable. Output complete YAML.",
  "gold_yaml_path": "examples/demo_virustotal_ip.yaml",
  "notes": "connector call + jinja path + Decision"
}
